用户偏好使用中文交流，回复尽量简洁以节省 Context。
§
凡是和时间相关的信息，用户要求明确标注 UTC 或北京时间（UTC+8）。
§
用户希望后续涉及 git 提交的流程默认执行 push。
§
用户偏好把单一 agent 型 cron 作为 testnet 交易全流程的主执行者，并把每日 runtime replay 诊断保持为只读分析；讨论定时任务时，用户希望明确区分“计划触发时间、实际执行完成时间、消息投递时间”，并关注每次 cron 的实际耗时。